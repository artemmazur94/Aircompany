@model Aircompany.Web.Models.Plane.PlaneViewModel

@{
    ViewBag.Title = "Plane details";
}

<div class="page-header">
    <h1>@(String.IsNullOrEmpty(Model.Name) ? Model.Name : $"{Model.Manufacturer} {Model.PlaneModel}")</h1>
</div>

<div class="row">
    <div class="col-sm-3">
        @if (Model.PhotoEntity == null)
        {
            <img class="img-responsive" src="@Url.Content("~/Content/images/default_plane_image.png")"/>
        }
        else
        {
            <img class="img-responsive" src="@Url.Content($"~/{Model.PhotoEntity.Path}")" />
        }
    </div>
    <div class="col-sm-9">
        <div class="row">
            @Html.LabelFor(x => x.Name, new { @class = "control-label col-sm-3" })
            @Model.Name
        </div>
        <div class="row">
            @Html.LabelFor(x => x.Manufacturer, new { @class = "control-label col-sm-3" })
            @Model.Manufacturer
        </div>
        <div class="row">
            @Html.LabelFor(x => x.PlaneModel, new { @class = "control-label col-sm-3" })
            @Model.PlaneModel
        </div>
        <div class="row">
            @Html.LabelFor(x => x.MaxSpeed, new { @class = "control-label col-sm-3" })
            @Model.MaxSpeed km/h
        </div>
        <div class="row">
            @Html.LabelFor(x => x.Description, new { @class = "control-label col-sm-3" })
            <div class="col-sm-9">
                @Model.Description
            </div>
        </div>
        <hr/>
        <div class="row">
            <div class="col-sm-4 right">
                <a href="@Url.Action("Index", "Plane")">< Back to Planes</a>
            </div>
        </div>
    </div>
</div>